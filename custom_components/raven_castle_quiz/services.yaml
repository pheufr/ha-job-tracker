add_player:
  description: Add a player to Raven Castle Quiz
  fields:
    name:
      description: Player full name
      example: "Alice Smith"
    alias:
      description: Player alias shown on the leaderboard
      example: "Ace"
    photo:
      description: Player photo URL or local path
      example: "/local/players/alice.jpg"

remove_player:
  description: Remove a player from Raven Castle Quiz permanently
  fields:
    entity_id:
      description: Any player entity ID
      example: "sensor.rc_quiz_player1"
    player_id:
      description: Player ID as an alternative to entity_id
      example: "player1"

enable_player:
  description: Enable a player for the current quiz
  fields:
    entity_id:
      description: Any player entity ID
      example: "sensor.rc_quiz_player1"
    player_id:
      description: Player ID as an alternative to entity_id
      example: "player1"

disable_player:
  description: Disable a player for the current quiz
  fields:
    entity_id:
      description: Any player entity ID
      example: "sensor.rc_quiz_player1"
    player_id:
      description: Player ID as an alternative to entity_id
      example: "player1"

add_points:
  description: Add points to a player's current round score
  fields:
    entity_id:
      description: Any player entity ID
      example: "sensor.rc_quiz_player1"
    player_id:
      description: Player ID as an alternative to entity_id
      example: "player1"
    points:
      description: Number of points to add
      example: 5

remove_points:
  description: Remove points from a player's current round score
  fields:
    entity_id:
      description: Any player entity ID
      example: "sensor.rc_quiz_player1"
    player_id:
      description: Player ID as an alternative to entity_id
      example: "player1"
    points:
      description: Number of points to remove
      example: 5

start_new_round:
  description: Move current round scores into totals and reset the current round

start_new_quiz:
  description: Reset all quiz scores to zero

reset_quiz:
  description: Reset all scores and disable all players